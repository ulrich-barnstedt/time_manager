{
  "name": "time_manager",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "start": "tsc --build && node build/index.js",
    "precompiled" : "node build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "terminal-kit": "^3.0.0",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.4.5"
  }
}
